# Environment files (contain credentials — never commit these)
.env

# Dependencies (reinstalled via npm ci / npm install)
node_modules/

# Build output (regenerated via npm run build)
dist/

# OS / editor junk
.DS_Store
Thumbs.db
